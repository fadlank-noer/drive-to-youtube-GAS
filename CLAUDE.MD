- Keep readme file name to "README.MD"

## Library Documentation

When working with specific libraries in this project, read the corresponding documentation from `.claude/`:

- **@clack/prompts** → Read `.claude/clack.md` when working with CLI prompts
- **EJS** → Read `.claude/ejs.md` when working with template rendering
- **Google Apps Script** → Read `.claude/google_app_script_lib.MD` when writing output.gs
- **Jest** → Read `.claude/jest.md` when writing unit tests

Always read the relevant library doc before implementing features that use it.